# karakaya1745.github.io

Canli TV kanal katalogu (`channels.json`, `stream_map.json`).

## Stream Health Bot — GitHub Actions

> **SUSPENDED (kullanici istegi):** Zamanlanmis botlar askıya alindi. `schedule:` / cron tetikleyicileri kapatildi.
> Manuel tetikleme hala mumkun: Actions → workflow → **Run workflow**.

| Workflow | Durum |
|----------|--------|
| stream-health | Askida (yalnizca `workflow_dispatch`) |
| discover-missing | Askida (yalnizca `workflow_dispatch`) |
| enrich-stream-map | Askida (yalnizca `workflow_dispatch`) |
| import-legal-channels | Askida (yalnizca `workflow_dispatch`) |
